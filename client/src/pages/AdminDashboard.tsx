import { useAuth } from "@/_core/hooks/useAuth";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { ShieldCheck, UserCog, Users, Search, Crown } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

export default function AdminDashboard() {
  const { user } = useAuth();
  const [search, setSearch] = useState("");
  const usersQuery = trpc.admin.users.useQuery(undefined, { enabled: user?.role === "admin" });
  const utils = trpc.useUtils();
  const setRole = trpc.admin.setRole.useMutation({ onSuccess: () => { toast.success("အသုံးပြုသူ role ပြောင်းပြီးပါပြီ"); void utils.admin.users.invalidate(); }, onError: (error) => toast.error(error.message || "လုပ်ဆောင်၍မရပါ") });
  const users = (usersQuery.data ?? []).filter((entry) => `${entry.name ?? ""} ${entry.email ?? ""}`.toLowerCase().includes(search.toLowerCase()));

  if (user?.role !== "admin") return <div className="flex min-h-[70vh] items-center justify-center"><Card className="max-w-md rounded-3xl border-[#e1e9e4] text-center shadow-sm"><CardContent className="p-10"><ShieldCheck className="mx-auto h-10 w-10 text-[#b16d65]" /><h1 className="mt-4 text-xl font-semibold">Admin access လိုအပ်ပါသည်</h1><p className="mt-2 text-sm text-[#78867e]">ဤစာမျက်နှာကို Admin Owner များသာ အသုံးပြုနိုင်ပါသည်။</p></CardContent></Card></div>;

  return <div className="min-h-screen bg-[#f6f8f7] text-[#17201d] -m-4 p-4 md:p-8"><div className="mx-auto max-w-[1280px] space-y-7"><header><div className="mb-2 flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.22em] text-[#a06c18]"><Crown className="h-4 w-4" /> Owner control center</div><h1 className="text-3xl font-semibold tracking-tight md:text-4xl">Admin Dashboard</h1><p className="mt-2 text-sm text-[#63716b]">ဝန်ထမ်းအကောင့်များ၊ access roles နှင့် စနစ်ကျန်းမာရေးကို စီမံပါ။</p></header><section className="grid gap-4 md:grid-cols-3"><Metric icon={<Users className="h-5 w-5" />} label="စုစုပေါင်းအသုံးပြုသူ" value={String(usersQuery.data?.length ?? 0)} tone="green" /><Metric icon={<ShieldCheck className="h-5 w-5" />} label="Admin Owner" value={String((usersQuery.data ?? []).filter((entry) => entry.role === "admin").length)} tone="purple" /><Metric icon={<UserCog className="h-5 w-5" />} label="Employee" value={String((usersQuery.data ?? []).filter((entry) => entry.role === "user").length)} tone="orange" /></section><Card className="rounded-3xl border-[#e1e9e4] shadow-sm"><CardHeader className="border-b border-[#edf1ee] bg-white"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-center"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#2c6e49]">Account management</p><CardTitle className="mt-1 text-xl">အသုံးပြုသူ အကောင့်များ</CardTitle></div><div className="relative md:w-72"><Search className="absolute left-3 top-2.5 h-4 w-4 text-[#9aa79f]" /><Input className="pl-9" placeholder="အမည် / email ရှာရန်" value={search} onChange={(e) => setSearch(e.target.value)} /></div></div></CardHeader><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-sm"><thead className="bg-[#f8faf8] text-left text-xs uppercase tracking-wide text-[#748079]"><tr><th className="px-6 py-3">အသုံးပြုသူ</th><th className="px-4 py-3">Email</th><th className="px-4 py-3">လက်ရှိ Role</th><th className="px-4 py-3">နောက်ဆုံးဝင်ရောက်မှု</th><th className="px-6 py-3 text-right">လုပ်ဆောင်ချက်</th></tr></thead><tbody className="divide-y divide-[#edf1ee]">{usersQuery.isLoading ? <tr><td colSpan={5} className="px-6 py-12 text-center text-[#839087]">အသုံးပြုသူများ ဖတ်နေပါသည်…</td></tr> : users.length === 0 ? <tr><td colSpan={5} className="px-6 py-12 text-center text-[#839087]">အသုံးပြုသူ မတွေ့ပါ</td></tr> : users.map((entry) => <tr key={entry.id} className="hover:bg-[#fbfdfb]"><td className="px-6 py-4"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-[#eaf3ed] font-semibold text-[#2c6e49]">{entry.name?.charAt(0) || "U"}</div><span className="font-semibold">{entry.name || "အမည်မရှိ"}</span></div></td><td className="px-4 py-4 text-[#78867e]">{entry.email || "—"}</td><td className="px-4 py-4"><Badge className={entry.role === "admin" ? "border-0 bg-[#f1ecfb] text-[#7651a8]" : "border-0 bg-[#e8f5eb] text-[#2c6e49]"}>{entry.role === "admin" ? "Admin Owner" : "Employee"}</Badge></td><td className="px-4 py-4 text-[#78867e]">{entry.lastSignedIn ? new Date(entry.lastSignedIn).toLocaleString("my-MM") : "—"}</td><td className="px-6 py-4 text-right"><Button variant="outline" size="sm" disabled={setRole.isPending} onClick={() => setRole.mutate({ id: entry.id, role: entry.role === "admin" ? "user" : "admin" })} className="border-[#dfe7e2]">{entry.role === "admin" ? "Employee ပြောင်း" : "Admin ပြောင်း"}</Button></td></tr>)}</tbody></table></div></CardContent></Card></div></div>;
}

function Metric({ icon, label, value, tone }: { icon: React.ReactNode; label: string; value: string; tone: "green" | "purple" | "orange" }) { const colors = { green: "bg-[#e8f5eb] text-[#2c6e49]", purple: "bg-[#f1ecfb] text-[#7651a8]", orange: "bg-[#fff4e3] text-[#a15f13]" }; return <Card className="rounded-3xl border-[#e1e9e4] shadow-sm"><CardContent className="flex items-center justify-between p-5"><div><p className="text-xs font-semibold uppercase tracking-wide text-[#839087]">{label}</p><p className="mt-2 text-3xl font-semibold">{value}</p></div><div className={`rounded-2xl p-3 ${colors[tone]}`}>{icon}</div></CardContent></Card>; }
